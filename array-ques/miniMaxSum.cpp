#include<bits/stdc++.h>
using namespace std;

void miniMax(vector<int>&arr){
    int n = arr.size();
    int sum = 0;
    int maxSum = 0;
    int minSum = 0;
    for(int i=0; i<n; i++){
        if(i == 0){
            continue;
        }
        
    }
    cout<<minSum<<" "<<maxSum<<endl;
}

int main(){
    vector<int>arr = {1,2,3,4,5};
    miniMax(arr);
}