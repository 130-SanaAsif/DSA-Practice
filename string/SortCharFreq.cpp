#include<bits/stdc++.h>
using namespace std;

string sortCharFreq(string s){
    string res = "";
    int cnt = 0;
    for(int i=0; i<s.size(); i++){
        if(s[i] == s[i+1]);
            cnt++;
    }
    
}

int main(){
    string s = "tree";
    string res = sortCharFreq(s);
    cout<<res<<endl;
    return 0;
}